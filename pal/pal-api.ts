/**
 * The PAL (programmable application logic) Api stores custom logic handlers (run in web workers with a Tiny namespace shoved in)
 * for app-defined routes.
 *
 * Only useable in secure contexts.
 */
